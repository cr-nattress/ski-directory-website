/**
 * @deprecated Import from '@shared/config' instead
 * Re-exports from new location for backwards compatibility.
 */
export {
  featureFlags,
  getFeatureFlag,
  isFeatureEnabled,
} from '@shared/config';
export type { FeatureFlag } from '@shared/config';
