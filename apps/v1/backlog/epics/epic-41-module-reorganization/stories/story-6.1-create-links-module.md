# Story 6.1: Create Links Module Structure

## Description
Create the Links module directory structure in `modules/links/`.

## Acceptance Criteria
- [ ] `modules/links/` directory created
- [ ] `modules/links/components/` directory created
- [ ] `modules/links/hooks/` directory created (if needed)
- [ ] `modules/links/index.ts` barrel export created
- [ ] Build passes

## Tasks

1. Create directory structure:
   ```
   modules/links/
   ├── components/
   ├── hooks/
   └── index.ts
   ```

2. Create empty `modules/links/index.ts`:
   ```typescript
   // Links module exports
   // Components will be added in subsequent stories
   ```

## Testing
- `npm run build` passes
- Directory structure exists

## Estimated Effort
5 minutes
