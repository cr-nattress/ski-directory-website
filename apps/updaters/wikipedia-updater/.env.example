# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Google Cloud Storage Configuration
GCS_BUCKET_NAME=sda-assets-prod
GCS_KEY_FILE=../../../gcp/service-account-key.json

# Optional: Rate limiting (ms between Wikipedia requests)
WIKIPEDIA_RATE_LIMIT_MS=500

# Optional: Dry run mode (set to 'true' to skip GCS uploads)
DRY_RUN=false
